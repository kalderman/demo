language: cpp

jobs:
  include:
    - os: linux
      dist: xenial
      compiler: gcc
      env: COMPILER=g++-7
      addons:
        apt:
          update: true
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - gcc-7
            - g++-7
            - xorg-dev

    - os: linux
      dist: xenial
      compiler: clang
      addons:
        apt:
          update: true
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - xorg-dev
            - libc++

    - os: osx 
      osx_image: xcode10.1

script:
  - sh build.sh -c $COMPILER -clobber -test
